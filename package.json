{
  "type": "module",
  "dependencies": {
    "simplex-noise": "^4.0.3",
    "three": "^0.167.1",
    "express": "^4.17.1",
    "cors": "^2.8.5",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "vite": "^5.4.2",
    "serve": "^14.2.1",
    "concurrently": "^9.1.0"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview",
    "prod": "vite build && vite preview --host 127.0.0.1 --port 8080",
    "serve": "serve -s dist -l 8080",
    "server": "node src/server/mapGenerator.js",
    "start": "concurrently \"npm run dev\" \"npm run server\""
  }
}
